\begin{figure}[p]
\begin{mathpar}
\inferrule
 {{\begin{array}{l}
    γ : \Context{}_n \\
    Γ : \Usages{γ}
  \end{array}}
  \and k : \Var{}_n
  \and {\begin{array}{l}
    σ : \Type{}    \\
    Δ : \Usages{γ}
  \end{array}}
}{Γ ⊢ t ∈ \fresh{σ} ⊠ Δ : \Set{}
}
\and \inferrule
 {{\begin{array}{l}
    γ : \Context{}_n \\
    Γ : \Usages{γ}
  \end{array}}
  \and t : \Inferable{}_n
  \and {\begin{array}{l}
    σ : \Type{}    \\
    Δ : \Usages{γ}
  \end{array}}
}{Γ ⊢ t ∈ σ ⊠ Δ : \Set{}
}
\and \inferrule
 {{\begin{array}{l}
    γ : \Context{}_n \\
    Γ : \Usages{γ}
  \end{array}}
  \and {\begin{array}{l}
    σ : \Type{}    \\
    t : \Inferable{}_n
  \end{array}}
  \and Δ : \Usages{γ}
}{Γ ⊢ σ  ∋ t ⊠ Δ : \Set{}
}
\end{mathpar}
\caption{Typing relations for \Var{}, \Inferable{}, \Checkable{} and \Pattern{}}
\end{figure}

\begin{figure}[p]
\begin{mathpar}
\inferrule
 {
}{Γ ∙ \fresh{σ} ⊢ \texttt{zero} ∈ σ ⊠ Γ ∙ \stale{σ}
}
\and \inferrule
 {Γ ⊢ k ∈ σ ⊠ Δ
}{Γ ∙ A ⊢ \texttt{suc}(k) ∈ σ ⊠ Δ ∙ A
}
\end{mathpar}
\caption{Typing rules for \Var{}\label{figure:deBruijn}}

\begin{mathpar}
\inferrule
 {Γ ⊢ k ∈ σ ⊠ Δ
}{Γ ⊢ \texttt{var}(k) ∈ σ ⊠ Δ
}
\and \inferrule
 {Γ ⊢ t ∈ σ ⊸ τ ⊠ Δ \and Δ ⊢ σ ∋ u ⊠ Θ
}{Γ ⊢ \texttt{app}(t, u) ∈ τ ⊠ Θ
}
\and \inferrule
 {Γ ⊢ t ∈ σ ⊕ τ ⊠ Δ \and
  {\begin{array}{l}
    Δ ∙ \fresh{σ} ⊢ ν ∋ l ⊠ Θ ∙ \stale{σ} \\
    Δ ∙ \fresh{τ} ⊢ ν ∋ r ⊠ Θ ∙ \stale{τ}
  \end{array}}
}{Γ ⊢ \texttt{case}~t~\texttt{return}~ν~\texttt{of}~l~\texttt{\%\!\%}~r ∈ ν ⊠ Θ
}
\and \inferrule
 {Γ ⊢ σ ∋ t ⊠ Δ
}{Γ ⊢ \texttt{cut}(t, σ) ∈ σ ⊠ Δ
}
\end{mathpar}
\caption{Typing rules for \Inferable{}\label{figure:infer}}

\begin{mathpar}
\inferrule
 {Γ ∙ \fresh{σ} ⊢ τ ∋ b ⊠ Δ ∙ \stale{σ}
}{Γ ⊢ σ ⊸ τ ∋ \texttt{lam}(b) ⊠ Δ
}
\and \inferrule
 {Γ ⊢ t ∈ σ ⊠ Δ \and σ ∋ p \leadsto{} δ
  \\\\ Δ \append{} \fresh{δ} ⊢ τ ∋ u ⊠ Θ \append{} \stale{δ}
}{Γ ⊢ τ ∋ \texttt{let}~p~\texttt{∷=}~t~\texttt{in}~u ⊠ Θ
}
\and \inferrule
 {Γ ⊢ σ ∋ t ⊠ Δ
}{Γ ⊢ σ ⊕ τ ∋ \texttt{inl}(t) ⊠ Δ
}
\and \inferrule
 {Γ ⊢ τ ∋ t ⊠ Δ
}{Γ ⊢ σ ⊕ τ ∋ \texttt{inr}(t) ⊠ Δ
}
\\ \inferrule
 {Γ ⊢ σ ∋ a ⊠ Δ \and Δ ⊢ τ ∋ b ⊠ Θ
}{Γ ⊢ σ ⊗ τ ∋ \texttt{prd}(a, b) ⊠ Θ
}
\and \inferrule
 {Γ ⊢ t ∈ σ ⊠ Δ
}{Γ ⊢ σ ∋ \texttt{neu}(t) ⊠ Δ
}
\end{mathpar}
\caption{Typing rules for \Checkable{}\label{figure:check}}

\begin{mathpar}
\inferrule
 {
}{σ ∋ \texttt{v} \leadsto{} σ ∙ []
}
\and \inferrule
 {σ ∋ p \leadsto{} γ \and τ ∋ q \leadsto{} δ
}{σ ⊗ τ ∋ (p \texttt{,} q) \leadsto{} δ \append γ
}
\end{mathpar}
\caption{Typing rules for \Pattern{}\label{figure:pattern}}
\end{figure}
